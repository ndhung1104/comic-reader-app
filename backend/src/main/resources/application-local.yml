spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/comic_reader}
    username: ${DB_USERNAME:comic_user}
    password: ${DB_PASSWORD:comic_pass}
    driver-class-name: org.postgresql.Driver
  flyway:
    validate-on-migrate: false
  jpa:
    show-sql: true
